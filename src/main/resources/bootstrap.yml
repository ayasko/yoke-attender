spring:
  application:
    name: attender-service
    
  cloud:
    config:
      discovery:
        enabled: true
        service-id: yoke-configserver
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8580/eureka